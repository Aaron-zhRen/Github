@model IEnumerable<AdminPortal.Models.OnBorad>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Counter.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IcmRouting.RoutingId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EnvrionmentId)
        </th>
       
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Counter.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IcmRouting.RoutingId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EnvrionmentId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ApplicationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ApplicationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ApplicationId })
        </td>
    </tr>
}

</table>
