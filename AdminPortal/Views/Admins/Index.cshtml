@model AdminPortal.Models.Admin

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section scripts {
    @Scripts.Render("~/bundles/app")
}



<div class="form-horizontal">

    @*<div class="col-md-4">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h2 class="panel-title">Admins</h2>
            </div>
            <div class="panel-body">
                <ul class="list-unstyled" data-bind="foreach: admins">
                    <li>
                        <strong><span data-bind="text: TenantName"></span></strong>
                        <small><a href="#" data-bind="click: $parent.getAdminDetail">Details</a></small>
                    </li>
                </ul>
            </div>
        </div>
        <div class="alert alert-danger" data-bind="visible: error"><p data-bind="text: error"></p></div>
    </div>
    <!--ko if:detail()-->
    <div class="col-md-4">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h2 class="panel-title">Detail</h2>
            </div>
            <table class="table">
                <tr><td>TenantName</td><td data-bind="text: detail().TenantName"></td></tr>
                <tr><td>AppType</td><td data-bind="text: detail().Type"></td></tr>
                <tr><td>AppGroupId</td><td data-bind="text: detail().AppGroupId"></td></tr>
            </table>
        </div>
    </div>
    <!--/ko-->*@

    <div class="col-md-12">
        <div class="glyphicon-adjust">
            <div class="panel-heading">
                <h2 class="panel-title">Add Team</h2>
            </div>

            <div class="panel-body">
                <form class="form-inline" data-bind="submit: addAdmin">
                    @*<div class="form-group">
                        <label for="inputAppGroup" class="col-sm-2 control-label">AppGroup</label>
                        <div class="col-sm-10">
                            <select data-bind="options:appgroups, optionsText: 'AppGroupName', value: newAdmin.AppGroup"></select>
                        </div>
                    </div>*@

                    <div class="form-group" data-bind="with: newAdmin">

                        <div class="form-horizontal">
                            <label for="inputTenantName" class="col-sm-2 control-label">TenantName</label>
                            <div class="col-sm-10">
                                <input type="text" class="col-sm-offset-3 form-control" id="inputTenantName" data-bind="value:Tenant" />
                            </div>

                       </div>
                        <div class="form-horizontal">
                            <label for="inputAppgroupName" class="col-sm-2 control-label">AppgroupName</label>
                            <div class="col-sm-10">
                                <input type="text" class="col-sm-offset-3 form-control" id="inputAppgroupName" data-bind="value:AppGroup" />
                            </div>
                            
                        </div>
                        <div class="form-horizontal">
                            <label for="inputAppType" class="col-sm-2 control-label">AppTypeName</label>
                            <div class="col-sm-10">
                                <input type="text" class="col-sm-offset-3 form-control" id="inputAppTypeName" data-bind="value:AppType" />
                            </div>
                           
                        </div>
                    </div>
                   
                </form>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-default">Save</button>
                    <button type="submit" class="btn btn-default">Cancle</button>
                </div>
            </div>
        </div>
    </div>
</div>